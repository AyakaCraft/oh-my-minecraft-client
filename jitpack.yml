# https://jitpack.io/docs/BUILDING/#custom-commands
before_install:
  - sdk install java 25.0.3-zulu
  - sdk use java 25.0.3-zulu
install:
  - ./gradlew preprocessResources
  - ./gradlew build publishToMavenLocal
